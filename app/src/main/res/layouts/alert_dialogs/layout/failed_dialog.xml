<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@drawable/rounded_alert_dialog"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="16dp">

    <ImageView
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_gravity="center"
        android:src="@drawable/img_dialog_failed"
        android:layout_marginBottom="15dp"/>

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/failedDialogTxt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="Aktivitas gagal dilakukan"
        android:textAppearance="@style/text_regular_red"
        android:textSize="16sp"
        android:layout_marginBottom="15dp"/>

    <com.example.skripsol.FunctionHelper.GetMaterial.GetButton
        android:id="@+id/failedDialogButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:cornerRadius="12dp"
        android:text="coba lagi"/>
</LinearLayout>